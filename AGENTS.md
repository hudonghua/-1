# AGENTS.md — Cursor Agent 工作规约

本仓库的 **Cursor Cloud Agent / 本地 Agent** 在执行任务时，**默认遵守以下约定**。
任何 Agent 在开始干活前应当先读本文件。

---

## 1. 启动行为：先加载记忆与技能

Agent 启动后，**先按顺序读取**以下文件（若存在），加载顺序的详细版本见 `docs/memory/CURSOR_AGENT_MEMORY.md`：

1. **`docs/memory/CURSOR_AGENT_MEMORY.md`** —— 总入口 / 索引（**必读**）
2. **编程纪律 / 安全协作规约**（顺序读）：
   - `docs/skills/safe-collab-workflow/SKILL.md`
   - `docs/skills/verify-before-answer/SKILL.md`
   - `docs/skills/understand-first/SKILL.md`
   - `docs/skills/backup-and-edit/SKILL.md`
   - `docs/skills/safe-restore/SKILL.md`
3. **GBK / 嵌入式**（任务涉及 GBK/GB2312/Keil/HMI/嵌入式 C 时必读）：
   - `docs/skills/safe-edit-gbk/SKILL.md`
   - `docs/skills/embedded-c-safe-edit/SKILL.md`
   - `docs/skills/gbk-garbled-comments/SKILL.md`
   - `docs/skills/fix-braces/SKILL.md`
   - `docs/skills/keil5-embedded-c/SKILL.md`
   - `docs/memory/feedback_gbk_file_modification.md`
4. **跨电脑同步**（涉及 "上传" / 工具集合并时）：
   - `docs/skills/external-record-continuity/SKILL.md`
   - `docs/skills/multi-computer-toolkit-merge/SKILL.md`
   - `docs/skills/work-continuity-sync/SKILL.md`
5. **任务相关 project / reference / feedback** 文件（按需）

文件**不存在**就跳过，**不要伪造内容**。

> 用户说 "恢复记忆" / "restore memory" 时，依次重读 1 → 5，并在回复里**明确告知已加载哪些文件**。

### Skills / Memory 的上游真源

`docs/skills/` 和 `docs/memory/` 下大部分内容**镜像自**：

[`hudonghua/codex-personal-toolkit`](https://github.com/hudonghua/codex-personal-toolkit)

那是用户的 Codex Personal Toolkit，跨多台电脑 (`t250c` / `DELL` 等) 同步技能与工作状态。本仓库只镜像与本仓库工作相关的部分。上游更新后，按 `docs/memory/CURSOR_AGENT_MEMORY.md` §4 的流程同步。

---

## 2. 分支与提交

- **永远不直接在 `main` 上提交**。
- 新工作创建分支：`cursor/<kebab-case-task-name>-906f`
  - 全部小写
  - 后缀固定 `-906f`
- 每个**逻辑独立的变更**一个 commit，不要把无关改动塞到一个 commit。
- commit message 用简洁祈使句（中英皆可），第一行不超过 72 字符。
- **禁止** `git push --force`、`git commit --amend`，除非用户明确要求。

---

## 3. 默认交付方式（云端 Agent 必须遵守）

每一轮任务结束、给用户总结之前：

1. `git add` 必要文件
2. `git commit` 写清楚改了什么
3. `git push -u origin <branch>`（失败按指数退避重试 4s/8s/16s/32s）
4. **创建或更新 Pull Request**（默认 Draft）
5. 然后再写总结回复

> 这是为了让用户在本地通过 `git pull` 就能拿到所有产出，**形成无感同步闭环**。

---

## 4. 目录约定

```
.
├── AGENTS.md                 # 本文件
├── docs/
│   ├── memory/               # 长期记忆 / 编程纪律 / 项目笔记
│   │   ├── CURSOR_AGENT_MEMORY.md
│   │   ├── 编程手册与纪律.md
│   │   ├── MEMORY.md
│   │   ├── project_*.md
│   │   ├── reference_*.md
│   │   └── feedback_*.md
│   └── skills/               # 可复用技能 / SOP
│       ├── embedded-c-safe-edit.md
│       ├── safe-edit-gbk.md
│       ├── backup-and-edit.md
│       └── verify-before-answer.md
└── scripts/
    ├── auto-pull.ps1                  # Windows 后台自动 git pull
    ├── install-auto-pull-task.ps1     # 注册为 Windows 任务计划
    └── README.md                      # 本地同步使用说明
```

将 memory / skills 文件提交到上述位置，云端 Agent **自动可读**，无需用户每次提醒。

---

## 5. 编辑代码的纪律（关键摘录）

- 编辑前**至少 Read 一次**目标文件。
- 修改 **GBK / GB2312 / 嵌入式 C / 工业 HMI** 文件时，遵循 `docs/skills/safe-edit-gbk.md` 与 `embedded-c-safe-edit.md`。
- 不写解释性废注释（"// import xxx" 这种）。
- 引入 lint 错误必须修。
- 不无故重命名公共 API、不动 license / 版权头。

---

## 6. 与用户沟通

- **默认使用简体中文**回复。
- 不臆造模型名 / 文件路径 / API。不知道就说不知道。
- 工具调用前不要写冒号引导句。
- 不在代码里留 "Generated by ..." 之类标记。

---

## 7. 不做的事

- 不force push、不amend、不删别人的分支。
- 不擅自切换主分支基线。
- 不读取/上传仓库外的私密路径（如 `C:\Users\...`、`/etc/...` 中的凭证）。
- 不对外服务发请求（除非用户明确要求）。

---

> 本文件本身也可以被 Agent 修改，但**修改 AGENTS.md 必须单独成一个 commit**，方便审计。
